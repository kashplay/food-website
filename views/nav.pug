nav
  ul.navigation
    li.nav-line
      a.white-anchors.active(href='#feature-section') FEATURES
    li.nav-line
      a.white-anchors(href='') DETAILS
    li.nav-line
      a.white-anchors(href='') REVIEWS
    li.nav-line
      a.white-anchors(href='/plans') PLANS
    li.nav-line
      if user
        a.white-anchors(href='') #{user.name}
        a.white-anchors(href="/logout") LOGOUT
      else
        a.white-anchors(href='/login') LOGIN
